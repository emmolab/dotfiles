# Root login profile for Midnight Signal
# Keep this tiny and explicit, similar in spirit to the Lawrence Systems layout.

[ -f "$HOME/.profile" ] && . "$HOME/.profile"
[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
